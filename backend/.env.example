# App
PORT=3000

# Database (must match root .env.example POSTGRES_* values when run via docker-compose)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=todo_user
DB_PASSWORD=todo_password
DB_NAME=todo_db
